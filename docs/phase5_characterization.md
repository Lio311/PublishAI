# אפיון שלב 5: מנגנון טיפול אוטומטי בהערות Reviewers והגשה מחדש (Resubmission)

בהתאם לקו המנחה שלנו - **0 שימוש בכלים בתשלום** - אפיון זה נבנה כך שישתמש בתשתיות קוד פתוח ובתשתיות שכבר בנינו (כמו ה-Inngest ו-Claude), ללא צורך בשירותי צד-שלישי יקרים.

## רקע
לאחר שהמאמר מוגש לכתב העת (מה שכבר פיתחנו - חיבורי ה-OJS/Wordpress), העורך שולח חזרה "Reviewer Comments". 
המשתמש צריך לתקן את המאמר ולייצר מסמך "Response to Reviewers" נקודתי.

## ארכיטקטורה וזרימת נתונים (Data Flow)

### 1. קליטת הערות ה-Reviewers (ללא עלות)
במקום להקים שרת דואר ייעודי (Mailhook) שעולה כסף, נוסיף למסך ה-Dashboard של המאמר כפתור **"התקבלו הערות עמיתים" (Received Reviewer Comments)**.
המשתמש פשוט יעתיק וידביק את הטקסט של המייל שקיבל אל תוך חלונית טקסט עשירה במערכת, או יעלה את קובץ ה-PDF שקיבל מהעורך.

### 2. הקמת סוכן חדש: `RebuttalAgent` (Point-by-Point Response)
* ברגע שהמשתמש מזין את ההערות, Inngest יזניק סוכן חדש לגמרי שמטרתו **לנתח את ביקורת העמיתים**.
* הסוכן יחלץ את ההערות ויסדר אותן במבנה של טבלה או רשימה.
* הסוכן ייצר טיוטה ראשונית של מסמך "Response to Reviewers", תוך שהוא מציע למשתמש איך להגיב על כל סעיף (למשל: "החוקר ביקש להוסיף מקורות על X - אני אוסיף פסקה במבוא").
* **נקודת אישור משתמש:** המשתמש יוכל לאשר/לשנות את אסטרטגיית התגובה לפני שהטקסט משתנה.

### 3. הרצה חוזרת של פייפליין הסוכנים (Resubmission Pipeline)
* לאחר אישור אסטרטגיית התגובה, המערכת תריץ מחדש חלק משרשרת הסוכנים שלנו:
  1. **Knowledge Agent:** יחפש מקורות חדשים אם ה-Reviewers דרשו.
  2. **Execution Agent:** יבצע את השינויים במאמר עצמו כדי לרצות את ה-Reviewers (בהתבסס על ההבטחות במסמך התגובה).
  3. **QA Agent:** יוודא שהשינויים לא שברו את העיצוב.

### 4. הגשה מחדש (Auto-Resubmission)
* המערכת תייצר "גרסה 2" (v2) של המאמר + קובץ "Response to Reviewers.docx".
* באמצעות ה-`SubmissionService` שבנינו, נשלח קריאת עדכון (Update Article) לאותו כתב עת ב-OJS או WordPress עם הקבצים המעודכנים.

## משימות לביצוע (Task List)
1. **Frontend:** הוספת מודל (Modal) להזנת הערות סוקרים במסך ניהול מאמר (ArtifactDashboard).
2. **Backend/Inngest:** יצירת פונקציית Inngest חדשה בשם `processResubmission`.
3. **AI Agents:** יצירת `RebuttalAgent` שיודע להתכתב עם הערות העורכים.
4. **Database:** עדכון סכמת ה-DB לשמירת היסטוריית גרסאות (Review Rounds) ומסמכי Rebuttal.

## סיכום יתרונות הגישה
* **0 עלויות חודשיות קבועות:** אין צורך בשירותי קליטת אימיילים (כמו SendGrid Inbound Parse).
* **שקיפות מלאה:** המשתמש נשאר "In the loop" ומאשר את אסטרטגיית התגובה ל-Reviewer לפני שהמאמר משתנה.
