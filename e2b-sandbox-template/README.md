# E2B Data Science Template

This directory contains the custom Docker environment configuration for the Data Science Sandbox.

## Building and Pushing

To build and push this template to E2B, ensure you have the E2B CLI installed and authenticated. Then run the following command from the `e2b-sandbox-template` directory:

```bash
e2b template build
```
